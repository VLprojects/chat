import { makeStyles } from '@mui/styles';

export default makeStyles(
  {
    channelsList: {
      display: 'flex',
      flexDirection: 'column',
      flexGrow: 1,
    },
  },

  { name: 'ChannelList' },
);
