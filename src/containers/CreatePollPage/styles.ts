import { makeStyles } from '@mui/styles';

export default makeStyles({
  headerText: {
    marginTop: 0,
    marginBottom: 30,
    textAlign: 'center',
  },
  gridContainer: {
    padding: '32px 24px',
    overflow: 'auto',
  },
});
